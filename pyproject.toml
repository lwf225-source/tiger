[build-system]
requires = []
build-backend = "project_long_memory.build_backend"
backend-path = ["."]

[project]
name = "project-long-memory"
version = "2.2.0a3"
description = "Local-first, auditable project memory with optional code evidence"
requires-python = ">=3.9"
dependencies = []

[project.scripts]
plm = "project_long_memory.cli:main"
