interface:
  display_name: "Project Long Memory"
  short_description: "Auditable, project-scoped memory across Codex sessions"
  default_prompt: "Use $project-long-memory to recall and update durable project context with sources."
policy:
  allow_implicit_invocation: true
